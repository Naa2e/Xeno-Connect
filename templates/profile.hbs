<div class="name" "col-md-offset-6 ">Hey there Sexy
    </div>
<div class="container">
    <div class="panel panel-info">
        <nav>
            <ul class="nav nav-justified nikki">
                <li id="profile-nav"><a href="#">Profile</a></li>
                <li id="member-nav"><a href="#">Interstellar Matches</a></li>
            </ul>
        </nav>
        <div class="panel-body">
                {{#each members}}
                <div class="col-md-4" id="members">
                    <img src="{{ photo }}" id="pic">
                    <p>Name: {{ name }}</p>
                    <p>Classification: {{ classification}}</p>
                    <p>Atmospheric requirements: {{ atmospheric_requirements }}</p>
                    <p>Likes: {{ likes }}</p>
                    <p>Dislikes: {{ dislikes }}</p>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
</div>
</div>
</div>

